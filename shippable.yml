language: go

go:
  - 1.9

build:
  ci:
    - echo $BASE_BRANCH
    - echo $BRANCH
    - echo $GIT_TAG_NAME
    - echo $IS_RELEASE
    - if [[ "$GIT_TAG_NAME" == deploy* ]] ; then chmod +x deploy.sh && ./deploy.sh; fi;
    - echo "Done"